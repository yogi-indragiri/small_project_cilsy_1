pipeline {
    agent any

    stages {
        stage('Build Image Docker') {
            steps {
                echo 'Build Docker'
            }
        }
        stage('Push Image Docker') {
            steps {
                echo 'Push Image'
            }
        }
        stage('Deploy Kubernetes') {
            steps {
                echo 'Deploy Kubernetes'
            }
        }
    }
}
